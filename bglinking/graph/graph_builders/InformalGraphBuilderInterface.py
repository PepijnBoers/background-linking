#
# Works as an informal interface in Python.
# 

class InformalGraphBuilderInterface:
    def build(self,graph, cursor, embeddings, index_utils, docid, use_entities, nr_terms, term_tfidf, term_position, text_distance, term_embedding):
        """Build (initialize) graph."""
        pass
